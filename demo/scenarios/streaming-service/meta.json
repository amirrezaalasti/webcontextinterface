{
  "id": "streaming-service",
  "title": "StreamVault",
  "icon": "🎬",
  "difficulty": "Hard",
  "description": "Streaming hero and documentary tiles with runtime metadata.",
  "challenges": [
    "Play vs confirm",
    "data-runtime filter",
    "Dark theme + filler"
  ],
  "task": {
    "goal": "Add the fifty-two minute documentary titled Ocean Deep to the watchlist (not play trailer).",
    "standardSteps": [
      {
        "action": "scan",
        "target": "full DOM",
        "outcome": "confused",
        "note": "Many decoy buttons, ads, and domain-specific layout noise"
      },
      {
        "action": "click",
        "target": "decoy element",
        "outcome": "fail",
        "note": "Clicks promotional or nav decoy"
      },
      {
        "action": "click",
        "target": "[data-title-id=\"ocean-deep\"][data-runtime=\"52\"] .sv-btn--primary",
        "outcome": "success",
        "note": "Finds correct primary CTA in page-specific structure"
      }
    ],
    "agentdomSteps": [
      {
        "action": "read",
        "target": "streaming_service-page",
        "outcome": "success",
        "note": "Structured page landmark with typed actions"
      },
      {
        "action": "click",
        "target": "add-ocean-deep-watchlist",
        "outcome": "success",
        "note": "Direct WCI node click on annotated overlay"
      }
    ]
  },
  "groundTruth": {
    "wciNodeId": "add-ocean-deep-watchlist",
    "acceptableNodeIds": [],
    "decoyNodeIds": [
      "decoy-extra"
    ],
    "rawSelectors": [
      "[data-title-id=\"ocean-deep\"][data-runtime=\"52\"] .sv-btn--primary",
      "[data-title-id=\"ocean-deep\"] button.sv-btn--primary"
    ]
  }
}
