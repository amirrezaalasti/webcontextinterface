{
  "id": "email-client",
  "title": "InboxPro Mail",
  "icon": "📧",
  "difficulty": "Extreme",
  "description": "Email client table layout with generic toolbar Confirm for archive action.",
  "challenges": [
    "billing-acme row vs toolbar",
    "Reply/Forward duplicates",
    "Keyword archive decoys"
  ],
  "task": {
    "goal": "Archive the unread thread whose sender domain is acme.com billing (not digest or social notifications).",
    "standardSteps": [
      {
        "action": "scan",
        "target": "full DOM",
        "outcome": "confused",
        "note": "Many decoy buttons, ads, and domain-specific layout noise"
      },
      {
        "action": "click",
        "target": "decoy element",
        "outcome": "fail",
        "note": "Clicks promotional or nav decoy"
      },
      {
        "action": "click",
        "target": "[data-action-bar=\"thread-tools\"] .ip-btn--primary",
        "outcome": "success",
        "note": "Finds correct primary CTA in page-specific structure"
      }
    ],
    "agentdomSteps": [
      {
        "action": "read",
        "target": "email_client-page",
        "outcome": "success",
        "note": "Structured page landmark with typed actions"
      },
      {
        "action": "click",
        "target": "archive-billing-thread",
        "outcome": "success",
        "note": "Direct WCI node click on annotated overlay"
      }
    ]
  },
  "groundTruth": {
    "wciNodeId": "archive-billing-thread",
    "acceptableNodeIds": [],
    "decoyNodeIds": [
      "decoy-promo",
      "decoy-extra"
    ],
    "rawSelectors": [
      "[data-action-bar=\"thread-tools\"] .ip-btn--primary",
      "[data-action-bar=\"thread-tools\"] button.ip-btn--primary"
    ]
  }
}
